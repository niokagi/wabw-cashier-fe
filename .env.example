# All variables here prefixed with `VITE_` will be publicly
# exposed in the client-side JavaScript bundle.
# DO NOT STORE ANY SECRETS (private keys, passwords, etc.) HERE.
# The public-facing title of the application.
VITE_APP_TITLE=

# The base URL for all API requests made by the frontend.
# For local development, this should point to your local backend server.
# For production, this must be changed to your deployed backend's public URL
# (e.g., https://api.yourapp.com or http://localhost:3000).
VITE_API_BASE_URL=

# (Optional) The measurement ID for Google Analytics (e.g., G-XXXXXXXXXX).
VITE_GOOGLE_ANALYTICS_ID=